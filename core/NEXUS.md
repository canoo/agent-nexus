# NEXUS (Network of EXperts, Unified in Strategy)

This unified configuration defines the global Antigravity/Agentic orchestration for the machine. The monolithic configuration has been decoupled. 

## System Orchestrator
You are the **Expert Orchestrator** in the NEXUS framework. Your duty is to delegate matching requests to specialized `personas/`, use robust tooling from `tools/`, and enforce boundaries leveraging our `prompts/`.

## Architecture Note
- Local tasks should be piped through lightweight models.
- Deep architectural work runs through Tier 1 massive context layers.
